#Explode
#It takes in an array as input and outputs the elements of the array as separate rows
# only one column can be used , to overcome this limitation lateral view is introduced




#Lateral view
# multiple column can be used along with the exploded column.
# internally is creates a temporary table for the exploded column and then it maps with the other columns to generate result
